# This is my minor Js Projec which takes 1 day/5 hour to builld.


For the Js:
For getting a decimal value on JavaScript:
displaySelect.innerText=ResultStore.toFixed(2);


innerText is the string, if we try to divide without number it can give the NAN error make sure to avoid in future.


For Keyword button:
 if(event.key=='+'){
    displaySelect.innerText='+';
  }


For getting the what we've click we can get with the event.target




#      equalSelect.click(): For making a click function without calling a html.




When checking is this a newheight greater than the original Height:
  if(displaySelect.scrollHeight>displaySelect.clientHeight)